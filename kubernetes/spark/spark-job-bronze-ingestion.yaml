###############################################################################
# SparkApplication — Bronze Layer Ingestion
#
# Reads raw data from Kafka topics & batch files, processes with Spark,
# and writes partitioned Delta Lake tables into the Bronze Lakehouse.
#
# Configured with Promtail & Prometheus annotations for logging & metrics.
###############################################################################

apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata:
  name: spark-bronze-ingestion
  namespace: ecommerce-platform
  labels:
    app.kubernetes.io/name: spark
    app.kubernetes.io/component: bronze-ingestion
    app.kubernetes.io/part-of: ecommerce-data-platform
spec:
  type: Python
  pythonVersion: "3"
  mode: cluster
  image: ghcr.io/saranyarajagopal/ecommerce-platform-spark:latest
  imagePullPolicy: IfNotPresent
  mainApplicationFile: local:///workspace/pipeline/metadata/bronze.py
  sparkVersion: "3.5.1"
  restartPolicy:
    type: OnFailure
    onFailureRetries: 3
    onFailureRetryInterval: 10
    onSubmissionFailureRetries: 5
    onSubmissionFailureRetryInterval: 20

  driver:
    cores: 1
    coreLimit: "1200m"
    memory: "512m"
    labels:
      version: 3.5.1
      role: driver
      app: spark-bronze-ingestion
    annotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "4040"
      promtail.io/scrape: "true"
    serviceAccount: spark
    envFrom:
      - configMapRef:
          name: spark-config

  executor:
    cores: 1
    instances: 2
    memory: "512m"
    labels:
      version: 3.5.1
      role: executor
      app: spark-bronze-ingestion
    annotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "4040"
      promtail.io/scrape: "true"
    envFrom:
      - configMapRef:
          name: spark-config
